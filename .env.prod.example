# ===== Persona Lab SaaS — Production Environment =====

# --- Global ---
ENVIRONMENT=production
NODE_ENV=production

# --- Backend (backend-node) ---
PORT=3005

# Use managed Postgres in production if possible
# Example: postgresql://USER:PASSWORD@HOST:5432/DB
DATABASE_URL=postgresql://personalab:personalab123@postgres:5432/personalab

# Redis cache
REDIS_URL=redis://redis:6379/0

# Auth & security (generate unique, long secrets)
JWT_SECRET=change_me_in_prod
JWT_REFRESH_SECRET=change_me_in_prod
NEXTAUTH_SECRET=change_me_in_prod

# Public/Frontend URLs
# If serving frontend at https://app.example.com
FRONTEND_URL=https://app.example.com

# CORS (comma-separated origins)
CORS_ORIGIN=https://app.example.com
ALLOWED_ORIGINS=https://app.example.com

# Admin & flags
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=strong_password_here
ADMIN_MODE=false
BYPASS_BILLING=false

# Stripe (set real keys in prod)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_MODE=LIVE
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# LLM providers
AI_PROVIDER=openai
OPENAI_API_KEY=
MISTRAL_API_KEY=
OLLAMA_API_URL=

# Google OAuth (required for Google sign-in)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
BACKEND_URL=https://api.personalabai.com

# Optional service URLs
FASTAPI_URL=
NODE_BACKEND_URL=

# --- Frontend (Next.js) ---
# If reverse proxy maps /api to backend-node on the same domain, you can omit these.
NEXTAUTH_URL=https://app.example.com

# Either rely on same-origin /api, or set explicit API origin
# NEXT_PUBLIC_API_URL=https://app.example.com/api
# NEXT_PUBLIC_API_ORIGIN=https://app.example.com
# NEXT_PUBLIC_WS_URL=wss://app.example.com

# Public environment tag
NEXT_PUBLIC_ENVIRONMENT=production

# Logging
LOG_LEVEL=info

